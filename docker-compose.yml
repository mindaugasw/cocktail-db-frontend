version: '3.8'
services:
  builder:
    container_name: cocktail-db-frontend-builder
    build:
      context: .
      dockerfile: Dockerfile
    image: cocktail-db-frontend-img
    ports:
      - '5173:5173'
    volumes:
      - .:/home/node/app
